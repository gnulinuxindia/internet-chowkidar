post:
  summary: "Increment current counter value"
  operationId: incrementCount
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../../schemas/request/increment.yaml"
  responses:
    "200":
      description: "OK"
      content:
        application/json:
          schema:
            $ref: "../../schemas/response/counter.yaml"
